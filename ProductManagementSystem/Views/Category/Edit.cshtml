@model ProductManagementSystem.Models.Category

@{
    ViewBag.Title = "Edit Category";
}

<div class="panel panel-default">
    <div class="panel-heading">
        <h2 class="panel-title">Edit Category</h2>
    </div>
    <div class="panel-body">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.CategoryID)

            <div class="form-horizontal">
                <div class="form-group">
                    <label for="CategoryName" class="control-label col-md-2 required-field">Category Name</label>
                    <div class="col-md-10">
                        @Html.TextBox("CategoryName", Model.CategoryName, new { @class = "form-control", required = "required" })
                    </div>
                </div>

                <div class="form-group">
                    <label for="Description" class="control-label col-md-2">Description</label>
                    <div class="col-md-10">
                        @Html.TextArea("Description", Model.Description, new { @class = "form-control", rows = "4" })
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save" class="btn btn-primary" />
                        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" })
                    </div>
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Form validation
            $('form').submit(function (e) {
                var isValid = true;
                
                // Validate category name
                if ($('#CategoryName').val() === '') {
                    alert('Please enter a category name.');
                    isValid = false;
                }
                
                return isValid;
            });
        });
    </script>
} 